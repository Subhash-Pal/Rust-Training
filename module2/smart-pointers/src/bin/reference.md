/*```
```
| Smart Pointer | Use Case                         |
| ------------- | -------------------------------- |
| `Box<T>`      | Heap allocation, recursive types |
| `Rc<T>`       | Shared ownership (single-thread) |
| `Arc<T>`      | Thread-safe shared ownership     |
| `RefCell<T>`  | Runtime-checked borrow rules     |

```
*/
